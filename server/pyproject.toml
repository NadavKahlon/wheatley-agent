[project]
name = "wheatley"
version = "0.1.0"
description = "Wheatley Command & Control Server"
requires-python = ">=3.11"
dependencies = [
    "asyncio>=4.0.0",
    "ipython>=9.10.1",
    "loguru>=0.7.3",
]

[project.scripts]
wheatley-server = "wheatley.main:start"
